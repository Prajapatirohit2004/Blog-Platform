# GEMINI_API_KEY: Optional API key for external service calls.
# Store secrets in a local .env file and do not commit them to source control.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this app is hosted.
# Used for self-referential links, callbacks, and API routing.
APP_URL="MY_APP_URL"
